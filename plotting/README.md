This is not actually the GitHub but a quick script I'm using to make a plot for
the paper.

For data, use `X_train_raw.npy` and `y_train_raw.npy` and note that we should be multiplying stuff by 1000 so that we get things in MILLIMETERS. Yeah, John is right on that.
